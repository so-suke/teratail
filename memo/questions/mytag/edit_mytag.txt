mytag編集機能
■簡易説明
編集といっても、削除できるようにするだけ。
(remove_my_tag())
削除アクションボタンを表示する。
	✔編集ボタン押下で表示させる。
	✔編集ボタンは完了ボタンにする
	✔完了ボタン押下→削除ボタンなくす→編集btnに変更
✔通常、特定のタグページへ遷移してしまうが、それをしないようにする。

■削除について
削除ボタン押下
	■対象タグIDを削除
		✔ajaxでmytagsのwhere(user_id, tag_id)削除する。
			✔ajax処理の成功を受けて、html要素を削除する。
